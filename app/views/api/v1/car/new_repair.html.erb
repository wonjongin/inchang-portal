<h1 class="tw-text-2xl">차량 정비내역 추가</h1>
<br>
<div data-controller="car">
  <%= form_with url: '/api/v1/car/create_repair', method: :post, class: "w-full", id: "newCarRepairForm" do |form| %>
    <div class="row g-2 mb-3 w-full">
      <%= label_in_form(form, '차번호') %>
      <%= form.text_field :number, class: "col form-control form-control-sm", value: @car.number %>
    </div>
    <div class="row g-2 mb-3 w-full">
      <%= label_in_form(form, '정비일') %>
      <%= form.date_field :repaired_at, class: "col form-control form-control-sm" %>
    </div>
    <div class="row g-2 mb-3">
      <%= label_in_form(form, '주행거리') %>
      <%= form.text_field :odo, class: "col form-control form-control-sm number-auto-comma" %>
    </div>
    <div class="row g-2 mb-3">
      <%= label_in_form(form, '정비소') %>
      <%= form.text_field :center, class: "col form-control form-control-sm" %>
    </div>
    <div class="row g-2 mb-3">
      <%= label_in_form(form, '정비내용') %>
      <%= form.text_field :desc, class: "col form-control form-control-sm" %>
    </div>
    <div class="row g-2 mb-3">
      <%= label_in_form(form, '정비금액') %>
      <%= form.text_field :price, class: "col form-control form-control-sm number-auto-comma" %>
    </div>
    <div class="row g-2 mb-3">
      <%= label_in_form(form, '비고') %>
      <%= form.text_field :footnote, class: "col form-control form-control-sm" %>
    </div>
  <% end %>
  <br>
  <br>
  <div class="btn-group btn-group-sm" role="group" aria-label="Basic example">
    <button class="btn btn-outline-primary" value="저장" id="saveBtn"
            data-controller="car"
            data-action="click->car#saveCarRepair"
            data-car-type-param="create"
            data-car-car-id-param="<%= @car.id %>"
    >저장
    </button>
    <%= render BasicButtonComponent.new(
      "btn btn-outline-primary", title: "목록", path: "/api/v1/car/repair_list/#{@car.id}") %>
  </div>
</div>
